import { Html } from '@react-three/drei'
import { FC } from 'react'

const ProjectHtml: FC = () => {
	return (
		<Html occlude={'blending'}>
			<p>Hey Mom!</p>
		</Html>
	)
}

export default ProjectHtml
