export type K_vector3Array = [number, number, number]
export type K_xyzCoord = { x: number; y: number; z: number }

export interface IPosAndRotControls {
	position: K_vector3Array
	rotation: K_vector3Array
}
